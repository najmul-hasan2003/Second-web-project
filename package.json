{
  "name": "auth-project",
  "version": "1.0.0",
  "description": "Auth system with Node.js and MongoDB",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "twilio": "^5.12.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
